import React from "react";
import Header from "./Header";
import Footer from "./Footer";

const Landing = (props) => {
  return (
    <>
      <Header props={props} />

      <Footer />
    </>
  );
};

export default Landing;
